# ── FPA Dashboard — environment template ──
# Copy to .env and fill in. PIN is OFF by default.
PIN_ENABLED=false
# Required only when PIN_ENABLED=true (numpad supports up to 6 digits):
DASHBOARD_PIN=
# Any long random string:
SESSION_SECRET=change-me
PORT=3000
